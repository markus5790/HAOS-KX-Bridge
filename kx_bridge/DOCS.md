# KX-Bridge

Enable LAN mode on the Anycubic Kobra X.

Open the KX-Bridge Web UI from the Home Assistant App page and add the printer
using its LAN IP address.

For OrcaSlicer use:
`http://HOME_ASSISTANT_IP:7125`

KX-Bridge data is persisted under the HA App data volume.
